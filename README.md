# Visual Primers for Product Leaders

Four static teaching modules (AI Foundations, ML Fundamentals, Spec-Driven Development, Agentic AI), served with GitHub Pages and embedded in Notion.

Each page is self-contained HTML with inline SVG; there is no build step for hosting. `build_site.py` is the script that converted the published Claude artifacts into these standalone pages.
